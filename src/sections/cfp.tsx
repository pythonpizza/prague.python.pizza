import { Container, Heading } from "theme-ui";

export const CFP = () => (
    <Container id="cfp" variant="smallContainer">
        <Heading>Call For Proposals is closed</Heading>
    </Container>
);
